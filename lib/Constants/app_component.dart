class AppComponent {
  // Use for submit button like login/logout ..
  static const submitButtonHeight = 45.0;

  // Use for continue button.
  static const continueButtonHeight = 35.0;

  // Use for textfield, textinput,..
  static const textInputHeight = 50.0;

  // Use for all component need cornerRadius.
  static const cornerRadius = 5.0;

// Use for apply padding on component between left and right of screen.
  static const paddingLeftRight = 15.0;

  // Use for apply padding on component between component-1 and component-2.
  static const paddingBottom = 10.0;

  // Use for apply on Text, TextField fontSize
  static const titleFontSize = 23.0;
  static const textFieldTitleFontSize = 18.0;
  static const subTitleFontSize = 16.0;
  static const buttonTextFontSize = 16.0;

  static const fontName = "Raleway";

  static const termAndCondictionURL =
      'https://www.shoprunback.com/legal-notice-en/';
  static const privacyAndPolicyURL =
      'https://www.shoprunback.com/privacy-policy/';
}
